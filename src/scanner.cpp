/*
#include <unordered_map>

#include "scanner.hpp"

enum ValueType {
    VAL_BOOL,
    VAL_NUM,
    VAL_STRING
};

struct Value {
    ValueType type;
    union as{
        bool boolean;
        double number;
    };
};
//convert strings to tokens

*/